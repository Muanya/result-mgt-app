import { Routes } from '@angular/router';
import { GradeDistributionComponent } from './grade-distribution/grade-distribution.component';

export const routes: Routes = [
    { path: '', component: GradeDistributionComponent },

];

